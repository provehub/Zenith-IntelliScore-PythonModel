This is the codebase that trains and serve Zenith IntelliScore Machine Learning

We installed the following Python Libraries. 

# pip install scikit-learn pandas numpy shap joblib
# pip install fastapi uvicorn pandas numpy shap joblib pydantic

having don more research we'll be using pydantic2 in our FastApi serving

# pip install fastapi uvicorn "pydantic>=2" joblib numpy pandas shap scikit-learn